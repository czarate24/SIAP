/*
 * File: app/view/InstitucionesWinViewController7.js
 *
 * This file was generated by Sencha Architect
 * http://www.sencha.com/products/architect/
 *
 * This file requires use of the Ext JS 6.5.x Classic library, under independent license.
 * License of Sencha Architect does not include license for Ext JS 6.5.x Classic. For more
 * details see http://www.sencha.com/license or contact license@sencha.com.
 *
 * This file will be auto-generated each and everytime you save your project.
 *
 * Do NOT hand edit this file.
 */

Ext.define('cartera.view.InstitucionesWinViewController7', {
    extend: 'Ext.app.ViewController',
    alias: 'controller.bancoswin',

    onNombreBChange: function(field, newValue, oldValue, eOpts) {
        field.setValue(newValue.toUpperCase());
    },

    onAfiliacionBChange: function(field, newValue, oldValue, eOpts) {
        field.setValue(newValue.toUpperCase());
    },

    onNumCtaBChange: function(field, newValue, oldValue, eOpts) {
        field.setValue(newValue.toUpperCase());
    },

    onClabeBChange: function(field, newValue, oldValue, eOpts) {
        field.setValue(newValue.toUpperCase());
    },

    onContactoBChange: function(field, newValue, oldValue, eOpts) {
        field.setValue(newValue.toUpperCase());
    },

    onCorreoBChange: function(field, newValue, oldValue, eOpts) {
        field.setValue(newValue.toUpperCase());
    },

    onbtnGuardarB: function(button, e, eOpts) {
        var win = button.up('window'),
            form   = Ext.getCmp('formBancos'),
            record = form.getRecord(),
            values = form.getValues();

        //console.log(form);
        console.log(values.id_banco);
        if(form.isValid()){
            if(values.id_banco>0){
                //Modificar Forma de Pago.
                //var recordLogin = Ext.getStore('StoreLogin').data.items[0];
                //values.id_usuarioAct = recordLogin.data.id_usuario;
                // record.set(values);
                // Ext.getStore('FormasPagoStore').sync();
                form.submit({
                    params: Ext.JSON.encode(values),
                    url: 'api/banco/actualiza',
                    waitMsg: 'Procesando información...',
                    success: function() {


                        Ext.MessageBox.show({
                            title : 'Información',
                            buttons : Ext.MessageBox.OK,
                            msg : 'Estatus '+values.nombre+' ha sido actualizada.',
                            icon : Ext.Msg.INFO

                        });
                        Ext.getStore('BancosStore').load();

                        win.close();
                    },
                    failure: function() {
                        // console.log('FAILURE CAPTURA ESTATUS');
                    }
                });
            }else{
                //Agregar Estatus Ingreso.
                if(Ext.getStore('BancosStore').findExact('nombre', values.nombre)!=-1){
                    Ext.MessageBox.show({
                        title : 'Advertencia',
                        buttons : Ext.MessageBox.OK,
                        msg : 'Ya Existe Banco: '+values.descripcion+'. Verifique...',
                        icon : Ext.Msg.ERROR
                    });
                }else{
                    // var recordLogin = Ext.getStore('StoreLogin').data.items[0];
                    //values.id_usuarioAct = recordLogin.data.id_usuario;
                    record = Ext.create('cartera.model.BancosModel');
                    record.set(values);
                    //Ext.getStore('FormasPagoStore').add(record);
                    //Ext.getStore('FormasPagoStore').sync();
                    //Ext.getCmp('id_insFP').setValue(Ext._id_institucion);
                    form.submit({
                        params: Ext.JSON.encode(values),
                        url: 'api/bancos/create',
                        waitMsg: 'Procesando información...',
                        success: function() {


                            Ext.MessageBox.show({
                                title : 'Información',
                                buttons : Ext.MessageBox.OK,
                                msg : 'Banco '+values.nombre+' ha sido agregado.',
                                icon : Ext.Msg.INFO

                            });
                            Ext.getStore('BancosStore').load();

                            win.close();
                        },
                        failure: function() {
                            //console.log('FAILURE CAPTURA INSTITUCION');
                        }
                    });


                }
            }

        }else{
            //Si el formulario no es válido
            Ext.MessageBox.show({
                title : 'Atencion',
                buttons : Ext.MessageBox.OK,
                msg : 'Ingrese todos los campos obligatorios',
                icon : Ext.Msg.ERROR
            });
        }
    },

    onbtnCancelarB: function(button, e, eOpts) {
        button.up('window').close();
    },

    onFormasPagoWinAfterRender: function(component, eOpts) {

    }

});
